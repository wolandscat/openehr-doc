
[cols="^1,3,5"]
|===

3+^h| abstract  *class DV_QUANTIFIED*

h|*Description*
2+a|Abstract class defining the concept of true quantified values, i.e. values which are not only
ordered but have a precise magnitude.

h|*Inherit*
2+|xref:representation:representation-data_types.adoc#_class_dv_ordered[DV_ORDERED]

h|
2+^h|*Feature Group "access"*

h|prop +
  0..1
|*magnitude_status*: xref:representation:representation-builtin_types.adoc#_class_string[String]
a|Optional status of magnitude with values:

* `"="` : magnitude is a point value
* `"<"` : value is < magnitude
* `">"` : value is > magnitude
* `"\<="` : value is \<= magnitude
* `">="` : value is >= magnitude
* `"~"` : value is approximately magnitude

If not present, assumed meaning is `"="` .

h|prop +
  0..1
|*accuracy*: xref:representation:representation-builtin_types.adoc#_class_any[Any]
a|Accuracy of measurement. Exact form of expression determined in descendants.

h|*Invariants*
2+a|

h|
2+a|magnitude_status_valid: ∃ magnitude_status ⇒ valid_magnitude_status(magnitude_status);


|===


[%autowidth.stretch]
|===

3+^h|  *class DV_INTERVAL<T:DV_ORDERED>*


h|
2+^h|*Feature Group "access"*

h|prop +
  0..1
|*lower*: T
a|Lower bound. lower bound.

h|prop +
  0..1
|*upper*: T
a|Upper bound. Upper bound.

h|prop +
  
|*lower_unbounded*: xref:representation:builtin_types-gen.adoc#_class_boolean[Boolean]
a|True if `lower` boundary open (i.e. = `-infinity`). lower boundary open (i.e. =
-infinity)

h|prop +
  
|*upper_unbounded*: xref:representation:builtin_types-gen.adoc#_class_boolean[Boolean]
a|True if `upper` boundary open (i.e. = `+infinity`). upper boundary open (i.e. =
+infinity)

h|prop +
  
|*lower_included*: xref:representation:builtin_types-gen.adoc#_class_boolean[Boolean]
a|True if `lower` boundary value included in range, if `not _lower_unbounded`. lower
boundary value included in range if not lower_unbounded.

h|prop +
  
|*upper_included*: xref:representation:builtin_types-gen.adoc#_class_boolean[Boolean]
a|True if `upper` boundary value included in range if `not _upper_unbounded`. upper
boundary value included in range if not upper_unbounded.

h| func
|*has* : xref:representation:builtin_types-gen.adoc#_class_boolean[Boolean] +
 +
  *post_cond* +
    valid_result: Result = (lower_unbounded ∨ lower_included ∧ v >= lower) ∨ v > lower ∧ (upper_unbounded ∨ upper_included ∧ v <= upper ∨ v < upper);

a|True if the value `e` is properly contained in this Interval. True if (lower_unbounded or
((lower_included and v >= lower) or v > lower)) and (upper_unbounded or ((upper_included
and v <= upper or v < upper)))

h| func
|*intersects* : xref:representation:builtin_types-gen.adoc#_class_boolean[Boolean]
a|True if there is any overlap between intervals represented by Current and `other`. True
if at least one limit of `other` is strictly inside the limits of this interval.

h| func
|*contains* : xref:representation:builtin_types-gen.adoc#_class_boolean[Boolean]
a|True if current interval properly contains `other`? True if all points of `other` are
inside the current interval.

h|*Invariants*
2+a|

h|
2+a|limits_consistent: (¬ upper_unbounded ∧ ¬ lower_unbounded) ⇒ (lower.is_strictly_comparable_to(upper) ∧ lower <= upper);


|===


[cols="^1,3,5"]
|===

3+^h| abstract  *class DV_ORDERED*

h|*Description*
2+a|Abstract class defining the concept of ordered values, which includes ordinals as well as
true quantities. It defines the functions `<` and `is_strictly_comparable_to()`, the
latter of which must evaluate to `True` for instances being compared with the `<`
function, or used as limits in the `DV_INTERVAL<T>` class.

Data value types which are to be used as limits in the `DV_INTERVAL<T>` class must inherit
from this class, and implement the function `is_strictly_comparable_to()` to ensure that
instances compare meaningfully. For example, instances of `DV_QUANTITY` can only be
compared if they measure the same kind of physical quantity.

h|*Inherit*
2+|xref:representation:representation-data_types.adoc#_class_data_value[DATA_VALUE],xref:representation:representation-builtin_types.adoc#_class_ordered[Ordered]

h|
2+^h|*Feature Group "access"*

h|prop +
  0..1
|*normal_status*: xref:representation:representation-data_types.adoc#_class_code_phrase[CODE_PHRASE]
a|Optional normal status indicator of value with respect to normal range for this value.
Often included by lab, even if the normal range itself is not included. Coded by ordinals
in series HHH, HH, H, (nothing), L, LL, LLL; see openEHR terminology group
`normal_status`.

h|prop +
  0..1
|*normal_range*: xref:representation:representation-data_types.adoc#_class_dv_interval[DV_INTERVAL]<xref:representation:representation-data_types.adoc#_class_dv_ordered[DV_ORDERED]>
a|Optional normal range.

h|prop +
  0..1
|*other_reference_ranges*: xref:representation:representation-builtin_types.adoc#_class_list[List]<xref:representation:representation-data_types.adoc#_class_reference_range[REFERENCE_RANGE]<xref:representation:representation-data_types.adoc#_class_dv_ordered[DV_ORDERED]>>
a|Optional tagged other reference ranges for this value in its particular measurement
context.

h| func
|*is_strictly_comparable_to* ( +
other: xref:representation:representation-data_types.adoc#_class_dv_ordered[DV_ORDERED]): xref:representation:representation-builtin_types.adoc#_class_boolean[Boolean]
a|Test if two instances are strictly comparable. Effected in descendants.

h| func
|*less_than* ( +
other: xref:representation:representation-data_types.adoc#_class_dv_ordered[DV_ORDERED]): xref:representation:representation-builtin_types.adoc#_class_boolean[Boolean] +
 +
  *pre_cond* +
    comparable: is_strictly_comparable_to(other);

a|True if this Ordered object is less than `other`. Redefined in descendants.

h| func
|*is_simple* : xref:representation:representation-builtin_types.adoc#_class_boolean[Boolean]
a|True if this quantity has no reference ranges.

h| func
|*is_normal* : xref:representation:representation-builtin_types.adoc#_class_boolean[Boolean] +
 +
  *pre_cond* +
    testable: ∃ normal_range ∨ ∃ normal_status;
 +
 +
  *post_cond* +
    range_normal: ∃ normal_range ⇒ Result = normal_range.has(self); +
    normal_asserted: ∃ normal_status ⇒ normal_status.code_string.is_equal("N");

a|Value is in the normal range, determined by comparison of the value to `normal_range` if
present, or by the `normal_status` marker if present.

h|
2+^h|*Feature Group "operators"*

h| func
|*less_than_or_equal* ( +
other: xref:representation:representation-builtin_types.adoc#_class_ordered[Ordered]): xref:representation:representation-builtin_types.adoc#_class_boolean[Boolean] +
  *alias* <=, ≤
 +
 +
  *post_cond* +
    valid_result: ¬ (other < self);

a|True if current object less than or equal to `other`.

h| func
|*greater_than* ( +
other: xref:representation:representation-builtin_types.adoc#_class_ordered[Ordered]): xref:representation:representation-builtin_types.adoc#_class_boolean[Boolean] +
  *alias* >
 +
 +
  *post_cond* +
    valid_result: Result = (other < self);

a|True if current object greater than `other`.

h| func
|*greater_than_or_equal* ( +
other: xref:representation:representation-builtin_types.adoc#_class_ordered[Ordered]): xref:representation:representation-builtin_types.adoc#_class_boolean[Boolean] +
  *alias* >=, ≥
 +
 +
  *post_cond* +
    valid_result: Result = (other <= self);

a|True if current object greater than or equal to `other`.

h|*Invariants*
2+a|

h|
2+a|other_reference_ranges_validity: ∃ other_reference_ranges ⇒ ¬ other_reference_ranges.is_empty();

h|
2+a|is_simple_validity: (¬ ∃ normal_range ∧ ¬ ∃ other_reference_ranges) ⇒ is_simple;

h|
2+a|normal_status_validity: ∃ normal_status ⇒ code_set(Code_set_id_normal_statuses).has_code(normal_status);

h|
2+a|normal_range_and_status_consistency: (∃ normal_range ∧ ∃ normal_status) ⇒ (normal_status.code_string.is_equal("N") ⊻ ¬ normal_range.has(self));


|===

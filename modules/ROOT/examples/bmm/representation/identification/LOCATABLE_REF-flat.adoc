
[cols="^1,3,5"]
|===

3+^h|  *class LOCATABLE_REF*

h|*Description*
2+a|Reference to a `LOCATABLE` instance inside the top-level content structure inside a
`VERSION<T>` identified by the `id` attribute. The `path` attribute is applied to the
object that `VERSION._data` points to.

h|*Inherit*
2+|xref:representation:representation-identification.adoc#_class_object_ref[OBJECT_REF]

h|
2+^h|*Feature Group "access"*

h|prop +
  0..1
|*path*: xref:representation:representation-builtin_types.adoc#_class_string[String]
a|The path to an instance, as an absolute path with respect to the object found at
`VERSION._data`. An empty path means that the object referred to by `id` is being
specified.

h|prop +
  
|*id*: xref:representation:representation-identification.adoc#_class_uid_based_id[UID_BASED_ID]
a|Globally unique id of an object, regardless of where it is stored.

h| func
|*as_uri* : xref:representation:representation-builtin_types.adoc#_class_string[String]
a|A URI form of the reference, created by concatenating the following:

* scheme, e.g. `ehr:`, derived from `namespace`
* `id.value`
* `/` + `path`, where `path` is non-empty

h|prop +
  
|*namespace*: xref:representation:representation-builtin_types.adoc#_class_string[String]
a|Namespace to which this identifier belongs in the local system context (and possibly in
any other openEHR compliant environment) e.g. terminology , demographic . These names are
not yet standardised. Legal values for `namespace` are:

* `"local"`
* `"unknown"`
* a string matching the standard regex `[a-zA-Z][a-zA-Z0-9_.:\/&?=+-]*`.

Note that the first two are just special values of the regex, and will be matched by it.

h|prop +
  
|*type*: xref:representation:representation-builtin_types.adoc#_class_string[String]
a|Name of the class (concrete or abstract) of object to which this identifier type refers,
e.g. `PARTY`, `PERSON`, `GUIDELINE` etc. These class names are from the relevant reference
model. The type name `ANY` can be used to indicate that any type is accepted (e.g. if the
type is unknown).

|===

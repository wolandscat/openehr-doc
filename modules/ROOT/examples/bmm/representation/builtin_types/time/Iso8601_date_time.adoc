
[cols="^1,3,5"]
|===

3+^h|  *class Iso8601_date_time*

h|*Description*
2+a|Represents an ISO 8601 date/time, including partial and extended forms. Value may be:

* `YYYY-MM-DDThh:mm:ss[(,\|.)sss][Z \| ±hh[:mm]]` (extended, preferred) or
* `YYYYMMDDThhmmss[(,\|.)sss][Z \|; ±hh[mm]]` (compact)
* or a partial variant.

See `valid_iso8601_date_time()` for validity.

Note that this class includes 2 deviations from ISO 8601:2004:

* for partial date/times, any part of the date/time up to the month may be missing, not
just seconds and minutes as in the standard;
* the time `24:00:00` is not allowed, since it would mean the date was really on the next
day.

h|*Inherit*
2+|xref:representation:representation-builtin_types.adoc#_class_iso8601_type[Iso8601_type]

h|
2+^h|*Feature Group "access"*

h| func
|*year* : xref:representation:representation-builtin_types.adoc#_class_integer[Integer]
a|Extract the year part of the date as an Integer.

h| func
|*month* : xref:representation:representation-builtin_types.adoc#_class_integer[Integer] +
 +
  *pre_cond* +
    month_known: ¬ month_unknown;

a|Extract the month part of the date/time as an Integer, or return 0 if not present.

h| func
|*day* : xref:representation:representation-builtin_types.adoc#_class_integer[Integer] +
 +
  *pre_cond* +
    day_known: ¬ day_unknown;

a|Extract the day part of the date/time as an Integer, or return 0 if not present.

h| func
|*hour* : xref:representation:representation-builtin_types.adoc#_class_integer[Integer] +
 +
  *pre_cond* +
    hour_known: ¬ hour_unknown;

a|Extract the hour part of the date/time as an Integer, or return 0 if not present.

h| func
|*minute* : xref:representation:representation-builtin_types.adoc#_class_integer[Integer] +
 +
  *pre_cond* +
    minute_known: ¬ minute_unknown;

a|Extract the minute part of the date/time as an Integer, or return 0 if not present.

h| func
|*second* : xref:representation:representation-builtin_types.adoc#_class_integer[Integer] +
 +
  *pre_cond* +
    second_known: ¬ second_unknown;

a|Extract the integral seconds part of the date/time (i.e. prior to any decimal sign) as an
Integer, or return 0 if not present.

h| func
|*fractional_second* : xref:representation:representation-builtin_types.adoc#_class_real[Real]
a|Extract the fractional seconds part of the date/time (i.e. following to any decimal sign)
as a Real, or return 0.0 if not present.

h| func
|*timezone* : xref:representation:representation-builtin_types.adoc#_class_iso8601_timezone[Iso8601_timezone]
a|Timezone; may be Void.

h| func
|*as_string* : xref:representation:representation-builtin_types.adoc#_class_string[String]
a|Return the string value in extended format.

h|*Invariants*
2+a|

h|
2+a|year_valid: valid_year(year);

h|
2+a|month_valid: valid_month(month);

h|
2+a|day_valid: valid_day(year, month, day);

h|
2+a|hour_valid: valid_hour(hour, minute, second);

h|
2+a|minute_valid: ¬ minute_unknown ⇒ valid_minute(minute);

h|
2+a|second_valid: ¬ second_unknown ⇒ valid_second(second);

h|
2+a|fractional_second_valid: has_fractional_second ⇒ (¬ second_unknown ∧ valid_fractional_second(fractional_second));

h|
2+a|partial_validity_year: ¬ month_unknown;

h|
2+a|partial_validity_month: ¬ month_unknown;

h|
2+a|partial_validity_day: ¬ day_unknown;

h|
2+a|partial_validity_hour: ¬ hour_unknown;

h|
2+a|partial_validity_minute: minute_unknown ⇒ second_unknown;


|===

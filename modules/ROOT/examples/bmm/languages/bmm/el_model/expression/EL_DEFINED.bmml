| 
| A predicate taking one external variable reference argument, that returns true if the
| reference is resolvable, i.e. the external value is obtainable.
| 
| NOTE: probably to be removed.
| 

class EL_DEFINED
    is_a EL_PREDICATE


end

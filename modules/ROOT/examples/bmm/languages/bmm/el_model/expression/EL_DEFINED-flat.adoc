
[%autowidth.stretch]
|===

3+^h|  *class EL_DEFINED*

h|*Description*
2+a|A predicate taking one external variable reference argument, that returns true if the
reference is resolvable, i.e. the external value is obtainable.

NOTE: probably to be removed.

h|*Inherit*
2+|xref:languages:bmm-gen.adoc#_class_el_predicate[EL_PREDICATE]

h|
2+^h|*Feature Group "access"*

h|prop +
  
|*operand*: xref:languages:bmm-gen.adoc#_class_el_value_generator[EL_VALUE_GENERATOR]
a|The target instance of this predicate.

h| func
|*eval_type* : xref:languages:bmm-gen.adoc#_class_bmm_simple_type[BMM_SIMPLE_TYPE]
a|Return `{BMM_MODEL}._boolean_type_definition_()`.

h| func
|*is_boolean* : xref:representation:builtin_types-gen.adoc#_class_boolean[Boolean] +
 +
  *post_cond* +
    result_validity: Result = eval_type().equal(BMM_MODEL.boolean_type_definition());

a|True if `eval_type` is notionally Boolean (i.e. a `BMM_SIMPLE_TYPE` with `type_name()`
= `Boolean`).

|===


[%autowidth.stretch]
|===

3+^h| abstract  *class BMM_FORMAL_ELEMENT*

h|*Description*
2+a|A formal element having a name, type and a type-based signature.

h|*Inherit*
2+|xref:languages:bmm-gen.adoc#_class_bmm_model_element[BMM_MODEL_ELEMENT]

h|
2+^h|*Feature Group "access"*

h|prop +
  
|*type*: xref:languages:bmm-gen.adoc#_class_bmm_type[BMM_TYPE]
a|Declared or inferred static type of the entity.

h|prop +
  
|*is_nullable*: xref:representation:builtin_types-gen.adoc#_class_boolean[Boolean]
a|True if this element can be null (Void) at execution time. May be interpreted as
optionality in subtypes..

h| func
|*signature* : xref:languages:bmm-gen.adoc#_class_bmm_signature[BMM_SIGNATURE]
a|Formal signature of this element, in the form:

`name [arg1_name: T_arg1, ...][:T_value]`

Specific implementations in descendants.

h| func
|*is_boolean* : xref:representation:builtin_types-gen.adoc#_class_boolean[Boolean] +
 +
  *post_cond* +
    result_valid: Result = type().equal(BMM_MODEL.boolean_type_definition());

a|True if `type` is notionally Boolean (i.e. a `BMM_SIMPLE_TYPE` with `type_name()` =
`'Boolean'`).

|===
